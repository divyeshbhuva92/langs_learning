// string functions...
// strlen = for string length.
// strrev = to reverse the string.
// strupr = to change string to uppercase
// strlwr = to change string to lowercase.
// strcpy = to copy one string to another.

#include <stdio.h>
#include <string.h>

int main(){
    char str[10];

    printf("Enter name: ");
    scanf("%s", str);

    printf("Entered string is %s\n", str);
    
    //----------------------------------------------------------------

    // int a = strlen(str);
    // printf("string length is %d.", a);

    //----------------------------------------------------------------

    // strrev(str);
    // printf("Revers string is %s\n", str);

    //----------------------------------------------------------------

    // strupr(str);
    // printf("string in uppercase is %s\n", str);

    //----------------------------------------------------------------

    // strlwr(str);
    // printf("string in lowercase is %s\n", str);

    //----------------------------------------------------------------
}